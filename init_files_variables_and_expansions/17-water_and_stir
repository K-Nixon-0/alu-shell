#!/bin/bash
python3 -c $'import sys\nw="water"\ns="stir"\nb="bestchol"\nW,S=sys.argv[1],sys.argv[2]\nwd=sum(w.index(c)*5**i for i,c in enumerate(W[::-1]))\nsd=sum(s.index(c)*4**i for i,c in enumerate(S[::-1]))\nt=wd+sd\nprint("".join(b[int(d)] for d in oct(t)[2:]))' "$WATER" "$STIR"
